# Default values for sanbao-admin.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
replicaCount: 1
image:
  repository: registry.qtclinics.com/sanbao-admin/v1.5.4
  tag: 89f6210
  pullPolicy: IfNotPresent
  pullSecrets: aliyun-registry-key
service:
  name: sanbao-admin
  type: LoadBalancer
  externalPort: 443
  internalPort: 80
  annotations:
    service.beta.kubernetes.io/alicloud-loadbalancer-ProtocolPort: 'https:443'
    service.beta.kubernetes.io/alicloud-loadbalancer-CertID: 1811066757327101_161e4ddaf08_-543902569_14586787
    service.beta.kubernetes.io/alicloud-loadbalancer-ChargeType: paybytraffic
    service.beta.kubernetes.io/alicloud-loadbalancer-HealthCheckFlag: 'off'
ingress:
  enabled: false
  # Used to create an Ingress record.
  hosts:
    - chart-example.local
  annotations:
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  tls:
    # Secrets must be manually created in the namespace.
    # - secretName: chart-example-tls
    #   hosts:
    #     - chart-example.local
resources: {}
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  # limits:
  #  cpu: 100m
  #  memory: 128Mi
  # requests:
  #  cpu: 100m
  #  memory: 128Mi
