workspace:
  base: /home
  path: app

pipeline:
  docker_build :
    image: plugins/docker
    dockerfile: pipeline/Dockerfile
    registry: registry.cn-shenzhen.aliyuncs.com/qtc_test/ 
    repo: reaction-admin 
    secrets: [ docker_username, docker_password ]
    tags:
      - latest
      
  deploy:
      image: reaction-admin
      commands:
      script:
        - cd /home/app
        - pwd
        - echo "hello world"
